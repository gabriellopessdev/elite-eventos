# Portaria (scan QR) — Implementation Plan

**Goal:** Portaria (`DOOR`) trava uma sessão `PUBLISHED`, lê o QR (câmera `jsQR` ou digitação) e vê um dos quatro resultados — válido / inválido / usado / sessão errada — consumindo `UNUSED` → `USED` na hora.

**Architecture:** Sem migration. `verifyTicketCode` (HMAC) já existe. `scanTicket` no domínio tickets: HMAC → achar ticket → comparar `eventId` → `updateMany WHERE status=UNUSED`. Rota `POST /events/:id/scan` só `DOOR`; os quatro estados são **200** + `{ outcome, seat? }`. Front: `/door` reusa `GET /events`, filtra data/título no cliente, campo + câmera, faixa de resultado, pausa 2s no mesmo código.

**Branch:** `feat/door`  
**Fora:** link compartilhável (#6), Evento pai+sessões (#7), beep/lanterna/histórico, nome do cliente na faixa.

---

### Task 1: ADR + ROADMAP

**Files:**
- Modify: `docs/DECISIONS.md` (após ADR-009)
- Modify: `docs/ROADMAP.md` linha da fatia 5

- [ ] Append **ADR-010 — Portaria: scan HMAC atômico por sessão**:
  - `POST /events/:id/scan` `{ code }`, `requireRole(DOOR)`.
  - Sessão = `Event` `PUBLISHED` (ADR-008). Arquivada/inexistente → 404.
  - Código vazio → 400. Org/cliente/anon → 401/403.
  - 200 `{ outcome: 'valid' | 'invalid' | 'used' | 'wrong_event', seat?: { row, number } }`.
  - Ordem: HMAC/lixo/id inexistente → `invalid` (não vazar se o UUID existe); `eventId` ≠ `:id` → `wrong_event` (mesmo `USED`); `USED` na sessão certa → `used`; senão `updateMany` `UNUSED`→`USED` → `valid` + assento.
  - UI: seletor + filtro data/título no cliente; câmera (`jsQR`) e campo na mesma tela; válido consome na hora; fica em `/door`; pausa 2s / ignora o mesmo `code`.
- [ ] Em `docs/ROADMAP.md`, fatia 5: status `⬜` permanece até o Done no browser; na coluna Done, nota “em `feat/door`”.
- [ ] Não mudar schema. `TicketStatus` `UNUSED`/`USED` já existe.

---

### Task 2: `scanTicket` no repo

**Files:**
- Modify: `api/src/tickets/repo.ts`
- Create: `api/tests/scan-repo.test.ts`

- [ ] Em `repo.ts` exportar:

  ```ts
  import { TicketStatus } from '@prisma/client';
  import { verifyTicketCode } from './qr.js';

  export type ScanOutcome = 'valid' | 'invalid' | 'used' | 'wrong_event';

  export type ScanResult = {
    outcome: ScanOutcome;
    seat?: { row: string; number: number };
  };

  export async function scanTicket({
    eventId,
    code,
  }: {
    eventId: string;
    code: string;
  }): Promise<ScanResult> {
    const ticketId = verifyTicketCode(code.trim());
    if (!ticketId) return { outcome: 'invalid' };

    const ticket = await prisma.ticket.findUnique({
      where: { id: ticketId },
      include: { seat: { select: { row: true, number: true } } },
    });
    if (!ticket) return { outcome: 'invalid' };

    if (ticket.eventId !== eventId) return { outcome: 'wrong_event' };

    if (ticket.status === TicketStatus.USED) return { outcome: 'used' };

    const updated = await prisma.ticket.updateMany({
      where: { id: ticketId, eventId, status: TicketStatus.UNUSED },
      data: { status: TicketStatus.USED },
    });
    if (updated.count !== 1) return { outcome: 'used' };

    return {
      outcome: 'valid',
      seat: { row: ticket.seat.row, number: ticket.seat.number },
    };
  }
  ```

- [ ] `scan-repo.test.ts`: upsert org/customer dedicados (`scan-repo-org@elite.local` / `scan-repo-cli@elite.local`); criar **duas** sessões + 1 assento `SOLD` com ticket cada (usar `signTicketId(id)` + `crypto.randomUUID()` como em `checkoutHold`). Casos:
  - código lixo / sig adulterada / uuid com HMAC de **outro** secret via string inventada → `invalid`; ticket no DB inalterado
  - HMAC ok, id que não existe no DB → `invalid`
  - ticket da sessão B no `eventId` A → `wrong_event` (também se B já `USED`)
  - `USED` na sessão A → `used`
  - `UNUSED` na sessão A → `valid` + `{ row, number }`; segundo `scanTicket` → `used`
  - `Promise.all` dois scans do mesmo `UNUSED` → um `valid`, um `used`; no DB um `USED`
- [ ] Limpar tickets antes de `event.deleteMany` (FK Restrict), no `afterEach`/`afterAll`.
- [ ] Run `cd api && npm test -- tests/scan-repo.test.ts`, expect pass.

---

### Task 3: Rota HTTP `POST /events/:id/scan`

**Files:**
- Modify: `api/src/events/routes.ts` (final do `eventRoutes`, depois de archive)
- Modify: `api/src/events/repo.ts` — reusar `getEvent` / checagem `PUBLISHED` (já retorna `null` se `ARCHIVED`)
- Create: `api/tests/scan.test.ts`

- [ ] Body `{ code?: unknown }`. Se `typeof code !== 'string'` ou `code.trim() === ''` → `400 { message: 'code is required' }`.
- [ ] `preHandler: requireRole(Role.DOOR)`.
- [ ] `const event = await getEvent(id)`; se `null` → `404 { message: 'Event not found' }` (não chamar `scanTicket`).
- [ ] Senão `return scanTicket({ eventId: id, code: parsed })` — sempre **200** com o `ScanResult`.
- [ ] Importar `scanTicket` de `../tickets/repo.js`.
- [ ] `scan.test.ts`: users `scan-http-org@` / `scan-http-cli@` / `scan-http-door@` (não reusar `org@elite.local`). Helpers: `createSession()`, `issueTicket({ eventId, seatId, userId })` via Prisma + `signTicketId`. Casos HTTP:
  - sem Bearer → 401; CUSTOMER/ORGANIZER → 403; DOOR + sessão ok → 200
  - evento inexistente / `ARCHIVED` → 404
  - `{ }` ou `{ code: '   ' }` → 400
  - lixo → 200 `{ outcome: 'invalid' }`
  - outra sessão → 200 `wrong_event`
  - segundo scan do mesmo código na sessão certa → 200 `used`
  - primeiro scan `UNUSED` → 200 `{ outcome: 'valid', seat: { row, number } }`
- [ ] Run `cd api && npm test -- tests/scan.test.ts tests/scan-repo.test.ts`, expect pass.

---

### Task 4: Client web `scanEvent`

**Files:**
- Modify: `web/src/events/api.ts` (junto de `listMyTickets` / `archiveEvent`)

- [ ] Tipos:

  ```ts
  export type ScanOutcome = 'valid' | 'invalid' | 'used' | 'wrong_event';

  export type ScanResult = {
    outcome: ScanOutcome;
    seat?: { row: string; number: number };
  };
  ```

- [ ] Função (200 com `outcome` **não** lança; 401/403/404/400 lançam `ApiError` como as outras):

  ```ts
  export async function scanEvent(
    eventId: string,
    code: string,
    accessToken: string,
  ): Promise<ScanResult> {
    const res = await fetch(apiUrl(`/events/${eventId}/scan`), {
      method: 'POST',
      headers: authHeaders(accessToken, true),
      body: JSON.stringify({ code }),
    });
    if (!res.ok) {
      throw new ApiError(
        await readErrorMessage(res, 'Não foi possível validar o ingresso'),
        res.status,
      );
    }
    return res.json() as Promise<ScanResult>;
  }
  ```

- [ ] Reusar `listEvents()` e `formatSessionWhen()` — sem endpoint novo. Proxy Vite `/events` já cobre `/events/:id/scan`.

---

### Task 5: Página `/door` — guarda, seletor, filtro, digitação

**Files:**
- Create: `web/src/door/DoorPage.tsx`
- Create: `web/src/door/DoorPage.test.tsx`
- Modify: `web/src/App.tsx` — trocar `Placeholder` de `/door` por `<DoorPage />`; remover `Placeholder` se sobrar só nele
- Modify: `web/src/App.test.tsx` — `/door` visitante/cliente/org/portaria

- [ ] Guarda (espelha `NewEventPage`, com `next`):
  - sem sessão → `<Navigate to="/login?next=/door" replace />`
  - `role !== 'DOOR'` → `<Navigate to="/events" replace />`
- [ ] Load: `listEvents()` no mount.
- [ ] UI (mobile-first, paleta marquee / `fieldInput` / `marqueeGlow`):
  - `<label>` sessão: `<select>` das `PUBLISHED` (`title` + `formatSessionWhen(startsAt)`); valor = `event.id`; placeholder “Escolha a sessão”.
  - Filtro **data** `<input type="date">` e **título** `<input>` (ambos opcionais). AND se os dois preenchidos. Dia da sessão = data **local** de `startsAt`:

    ```ts
    export function sessionDay(iso: string): string {
      const d = new Date(iso);
      const y = d.getFullYear();
      const m = String(d.getMonth() + 1).padStart(2, '0');
      const day = String(d.getDate()).padStart(2, '0');
      return `${y}-${m}-${day}`;
    }
    ```

    Título: `event.title.toLowerCase().includes(query.trim().toLowerCase())`.
  - Se a sessão selecionada sumir do filtro, limpar seleção + faixa.
  - Campo código + botão “Validar” **disabled** sem sessão ou código vazio.
  - Submit: `scanEvent(eventId, code, token)` → faixa; **limpar o input**.
- [ ] Faixa (`role="status"`), copy:
  - `valid` → `Válido · ${seat.row}${seat.number}` (ex. `Válido · B7`)
  - `invalid` → `Ingresso inválido`
  - `used` → `Já utilizado`
  - `wrong_event` → `Sessão errada`
- [ ] Trocar sessão no select → limpar faixa.
- [ ] Erro de rede/`ApiError` → texto “Não foi possível validar o ingresso” (não misturar com os 4 `outcome`).
- [ ] Testes (`vi.fn` em `listEvents` / `scanEvent`):
  - CUSTOMER em `/door` → acaba em cartaz (heading de `/events` ou path)
  - visitante → `/login`
  - DOOR: duas sessões “Duna” e “Oppenheimer”; filtrar título `Oppen` deixa um option; filtrar data que só uma tem
  - sem sessão, Validar disabled
  - colar código + Validar chama `scanEvent` com o id da sessão; mock `valid` + `seat: { row: 'B', number: 7 }` → texto `Válido · B7`; input vazio depois
  - mock `wrong_event` → `Sessão errada`
- [ ] `App.test.tsx`: `MemoryRouter` `initialEntries={['/door']}` — DOOR vê heading da porta (não o Placeholder “Próximas fatias”); CUSTOMER não vê scanner.
- [ ] Run `cd web && npm test -- src/door/DoorPage.test.tsx src/App.test.tsx`, expect pass.

---

### Task 6: Câmera `jsQR` + pausa 2s

**Files:**
- Create: `web/src/door/QrCamera.tsx`
- Create: `web/src/door/QrCamera.test.tsx`
- Modify: `web/src/door/DoorPage.tsx`
- Modify: `web/package.json` — `jsqr` + `@types/jsqr`

- [ ] `cd web && npm install jsqr` e `npm install -D @types/jsqr`.
- [ ] `QrCamera` props: `{ enabled: boolean; ignoreCode?: string | null; onCode: (code: string) => void }`.
  - `enabled === false` (sem sessão): não chama `getUserMedia`; sem `<video>`.
  - `enabled`: `navigator.mediaDevices.getUserMedia({ video: { facingMode: 'environment' }, audio: false })`; `<video playsInline muted autoPlay>` + canvas offscreen; loop `requestAnimationFrame` → `jsQR(imageData.data, width, height)`; se `result.data` e `result.data !== ignoreCode`, `onCode(result.data)`.
  - Falha de permissão / API ausente: texto “Câmera indisponível — cole o código.” Campo da Task 5 continua.
  - Cleanup unmount: `cancelAnimationFrame` + `track.stop()`.
- [ ] `DoorPage`: `ignoreCode` = último código enviado enquanto `Date.now() < pausedUntil` (2_000 ms após cada POST, câmera **ou** digitação). Enquanto pausado, não disparar outro `scanEvent` do mesmo código. Depois da pausa, o mesmo QR pode ir de novo (vai responder `used` se foi `valid`).
- [ ] Testes câmera: mock `navigator.mediaDevices.getUserMedia` reject → texto de fallback; mock stream + mock `jsQR` return `{ data: 'ticket.sig' }` é opcional/frágil — o mínimo é fallback + `DoorPage` ignora segundo `onCode` igual nos 2s (`vi.useFakeTimers()`).
- [ ] Run `cd web && npm test && npm run typecheck`, expect green.
- [ ] Run `cd api && npm test && npm run typecheck`, expect green.

---

### Task 7: Verificação manual + ROADMAP Done

**Files:**
- Modify: `docs/ROADMAP.md` — fatia 5 ✅ quando o checklist abaixo passar
- Modify: `README.md` só se faltar a rota da portaria (mínimo)

- [ ] Browser (porta `portaria@elite.local` / `door12345`):
  - Org publica **duas** sessões; cliente compra um ingresso em cada (`Math.random` ok ou repetir Pagar).
  - Portaria filtra / escolhe sessão A; cola o `code` do stub A → **Válido · assento**; segundo cola → **Já utilizado**.
  - Ainda em A, cola o código B → **Sessão errada**.
  - Código lixo → **Ingresso inválido**.
  - Câmera (celular ou webcam no QR da outra aba): um beep visual, não vira **usado** no frame seguinte (pausa 2s).
  - Logado como cliente, URL `/door` → cartaz.
- [ ] Não abrir PR até o checklist acima ok.

---

## Requirement → task map

| Acordo grill | Task |
|--------------|------|
| Sessão travada primeiro | 3 (404), 5 (select) |
| Lista `PUBLISHED` + filtro data/título (AND, cliente) | 5 |
| Sem hierarquia Evento+sessões | 1 (ADR-008 intacta) |
| Válido consome na hora (`updateMany` UNUSED) | 2, 3 |
| Câmera + campo na mesma tela | 5, 6 |
| Fica na `/door`; pausa 2s; limpa input; sem histórico | 5, 6 |
| Ordem HMAC → ticket → sessão → status | 2 |
| `USED` na sala errada = `wrong_event` | 2, 3 |
| `POST /events/:id/scan`, 200 + `outcome` | 3, 4 |
| `/door` só `DOOR` | 5 |
| Decoder `jsQR` + `getUserMedia`; iPhone não depende de BarcodeDetector | 6 |
| Link (#6) fora | — |

## Handoff

Executar: **inline** (este chat, task a task) ou **parallel** (API tasks 2–3 em série; web 4–6 depois da API).

Só começar código após verbo explícito (*implementa* / *pode codear* / *aplica o plano*).
